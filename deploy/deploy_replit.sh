#!/bin/bash
# Helper script for Replit deployment (upload repo there first)
echo "Set Replit run command to: streamlit run streamlit_app/streamlit_app.py --server.port=$PORT --server.enableCORS=false"
